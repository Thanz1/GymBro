# GymBro Docker Compose Secrets — Template cho docker-compose-multistage.yml
# Copy file này thành .env và điền giá trị thật (nếu cần thay đổi)
# File này được commit lên Git để người khác biết cần cấu trúc env như thế nào

SA_PASSWORD=GymBro@2024Password
RABBITMQ_USER=guest
RABBITMQ_PASS=guest